#include "Manager.h"

Manager::Manager(char firstTurn, Board board, Player whitePlayer, Player blackPlayer) : _currentTurn(firstTurn), _board(board), _whitePlayer(whitePlayer), _blackPlayer(blackPlayer) 
{
	// this->_currentTurn = firstTurn;
	// this->_board = board;
	// this->_whitePlayer = whitePlayer;
	// this->_blackPlayer = blackPlayer;
}